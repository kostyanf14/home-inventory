# Only needed when the web app is served from a different origin than the API
# (a static host, or a CDN in front of the built assets). Leave it unset when a
# reverse proxy serves both on one origin, or when using the Vite dev proxy.
VITE_API_BASE_URL=
